#include <stdio.h>

void main() {
    int num,tmp;
    long int sum =0;
    printf("Enter the number to find the sum of digit : ");
    scanf("%d",&num);
    tmp=num;
      for(int i=0;i<=num;i++){
      sum=sum+tmp%10;
      tmp=tmp/10;
     }
     printf("\n Sum is: %d\n",sum);
  
}
